# NoMethodError: undefined method `keys' for Array

# B. There is no method called keys for Array objects.

# The error specifically calls out the fact the keys is a method.
# It also says we are using an array which does not contain keys.